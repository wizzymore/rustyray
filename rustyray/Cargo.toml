[package]
name = "rustyray"
version = "0.1.0"
edition = "2021"

[dependencies]
rustyray-sys = { path = "../rustyray-sys" }
serde = { version = "1.0.217", features = ["derive"] }

[[example]]
name = "window"

[features]
default = ["serde"]
serde = []
